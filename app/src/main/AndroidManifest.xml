<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esprit.scluptfit">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScluptFIT"
        android:usesCleartextTraffic="true">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAuxCJro8AnPr5Q9i40M2GxNrMeATxUDes"/>

        <activity android:name=".views.activities.ExerciceActivity"></activity>
        <activity android:name=".views.activities.SignUpValideActivity" />
        <activity android:name=".views.activities.HomeActivity" />
        <activity android:name=".views.activities.SignUpActivity" />
        <activity android:name=".views.activities.LoginActivity" />
        <activity android:name=".views.activities.StartActivity" />
        <activity android:name=".views.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>